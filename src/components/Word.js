import React , {Component} from 'react';

export default class Word extends Component{
    render(){
        //const {en,vn,isMemorized}=this.state.word;

        return(
            <div>
        </div>
        );
    }
}